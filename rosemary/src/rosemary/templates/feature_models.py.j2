from app import db


class {{ feature_name | pascalcase }}(db.Model):
    id = db.Column(db.Integer, primary_key=True)

    def __repr__(self):
        return f'{{ feature_name | pascalcase }}<{self.id}>'
